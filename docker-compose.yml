services:
  db:
    image: postgres:13
    container_name: broadcast-db
    env_file:
      - postgres.env
    ports:
      - 5432:5432
    volumes:
      - broadcast-db:/var/lib/postgresql/data/

volumes:
  broadcast-db:
